"""YASC tools (plan P10; design section 2): `minimize` and `make_ortho`.

Each tool is a module with a `main(argv)` entry point, run as
`python3 -m yasc.tools.minimize ...` or `python3 -m yasc.tools.make_ortho ...`.
"""
